services:
  - type: web
    name: luminaiq-backend
    env: python
    rootDir: backend
    buildCommand: |
      apt-get update && apt-get install -y poppler-utils tesseract-ocr
      pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - fromGroup: luminaiq-env
